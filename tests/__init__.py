"""Test suite for GCP-Native Multi-Agent Scrum Team.

Covers unit and integration tests for ADK python tools (`tools/`), skill YAML frontmatter and
schema validation (`.agent/skills/`), and agent handoffs / circuit breaker state (`orchestration/`).
"""
